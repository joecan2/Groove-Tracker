# Secrets
.env

# Python
__pycache__/
*.pyc
venv/
.venv/
*.egg-info/
.pytest_cache/

# Vendored hardware driver (manually copied per docs/SETUP.md, not tracked)
# Must live at the project root (sibling to groove_tracker/), not nested
# inside the package — it's imported as a bare top-level module.
/waveshare_epd/

# Mock-mode output
mock_output/

# Temp audio recordings (see audio_capture.py TEMP_AUDIO_DIR)
.tmp_audio/

# OS cruft
.DS_Store
